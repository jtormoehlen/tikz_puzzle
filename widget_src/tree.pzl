def draw_tree(branch_length, level):
    if level > 0:
        myTurtle.forward(branch_length)
        myTurtle.right(45)
        draw_tree(branch_length / 2, level - 1)
        myTurtle.left(90)
        draw_tree(branch_length / 2, level - 1)
        myTurtle.right(45)
        myTurtle.backward(branch_length)
myTurtle.right(270)
myTurtle.backward(300)
draw_tree(300, 7)
else: #distractor
return turtle.forward(length / 2) #distractor